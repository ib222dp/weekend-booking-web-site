<?php

//curl_get_request("localhost:8080");

/*function curl_get_request($url){
    $ch = curl_init();
    curl_setopt($ch, CURLOPT_URL, $url);

    $data = curl_exec($ch);
    curl_close($ch); 
}*/

phpinfo();
